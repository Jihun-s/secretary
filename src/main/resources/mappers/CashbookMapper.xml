<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.softsociety.secretary.dao.CashbookMapper">

<!-- 내역 입력 -->
<insert id="insertTrans" parameterType="Transaction">
	INSERT INTO 
	        Secretary_Transaction
	        (trans_id
	        , cashbook_id
	        , user_email
	        , family_id
	        , trans_date
	        , trans_type
	        , trans_category1
	        , trans_category2
	        , trans_payee
	        , trans_memo
	        , trans_amount)
	    VALUES 
	        (Secretary_Trans_SEQ.nextval
	        , #{cashbook_id}
	        , #{user_id}
	        , #{family_id}
	        , #{trans_date}
	        , #{trans_type}
	        , #{trans_category1}
	        , #{trans_category2}
	        , #{trans_payee}
	        , #{trans_memo}
	        , #{trans_amount}
	        )
</insert>

</mapper>